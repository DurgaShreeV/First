/*No of digits without using loop using C*/
#include <stdio.h>
#include<string.h>
int main()
{
    int n,no_of_digits;
    scanf("%d%n",&n,&no_of_digits);       //--->Integer     
    //scanf("%s%n",s,&no_of_digits);        --->String
    printf("%d",no_of_digits);
    return 0;
}
